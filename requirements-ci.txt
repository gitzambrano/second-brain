# Dependências de CI e teste, com versão fixa.
#
# A lista vivia repetida em quatro jobs do workflow, sem versão. Duas
# consequências: um job podia divergir do outro por um `pip install` esquecido,
# e uma release nova de qualquer pacote podia deixar a CI vermelha num dia em
# que ninguém tinha tocado no repositório.
#
# Para subir uma versão: mude aqui, rode a suíte local e mande num commit que
# só faça isso — assim uma quebra de dependência fica separada de uma quebra
# de código no histórico.
pytest==8.4.2
ruff==0.16.5
PyYAML==6.0.3
PyMuPDF==1.27.2.3
beautifulsoup4==4.15.0
html5lib==1.1
playwright==1.60.0
Pillow==12.2.0
Unidecode==1.4.0
